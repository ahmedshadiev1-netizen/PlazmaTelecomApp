# 🚀 Локальная сборка APK - 100% рабочий способ

## ✅ Быстрый старт (Windows)

```bash
cd PlazmaTelecomApp
build-local.bat
```

**APK будет в:** `android/app/build/outputs/apk/debug/app-debug.apk`

## 📋 Что нужно установить

### 1. Node.js (версия 18+)
```bash
node --version
```
Если не установлен: https://nodejs.org/

### 2. Java JDK 17
```bash
java -version
```
Если не установлен: https://adoptium.net/temurin/releases/?version=17

### 3. Android Studio (для Android SDK)
- Установите Android Studio: https://developer.android.com/studio
- Откройте Android Studio → SDK Manager
- Установите:
  - Android SDK Platform 33
  - Android SDK Build-Tools 33.0.0
  - Android SDK Command-line Tools

### 4. Переменные окружения
Добавьте в переменные окружения Windows:
```
ANDROID_HOME=C:\Users\ВашеИмя\AppData\Local\Android\Sdk
JAVA_HOME=C:\Program Files\Java\jdk-17
```

## 🔧 Пошаговая сборка вручную

Если скрипт не работает, выполните вручную:

```bash
cd PlazmaTelecomApp

# 1. Установка зависимостей
npm install --legacy-peer-deps

# 2. Переход в android
cd android

# 3. Создание keystore (если нужно)
keytool -genkeypair -v -storetype PKCS12 ^
    -keystore app\debug.keystore ^
    -storepass android ^
    -alias androiddebugkey ^
    -keypass android ^
    -keyalg RSA -keysize 2048 -validity 10000 ^
    -dname "CN=Android Debug,O=Android,C=US"

# 4. Сборка APK
gradlew.bat assembleDebug

# 5. Проверка результата
dir app\build\outputs\apk\debug\app-debug.apk
```

## ❌ Если сборка падает

### Ошибка: "react-native-gradle-plugin not found"
**Решение:** Используйте локальную сборку - она работает без этого плагина.

### Ошибка: "Android SDK not found"
**Решение:** 
1. Установите Android Studio
2. Установите Android SDK через SDK Manager
3. Проверьте переменную `ANDROID_HOME`

### Ошибка: "Java not found"
**Решение:**
1. Установите JDK 17
2. Проверьте переменную `JAVA_HOME`
3. Перезапустите командную строку

## 📱 Установка APK на телефон

1. Включите "Отладка по USB" в настройках разработчика
2. Подключите телефон к компьютеру
3. Скопируйте APK на телефон
4. Откройте файл на телефоне и установите

Или используйте ADB:
```bash
adb install android\app\build\outputs\apk\debug\app-debug.apk
```

## ✅ Преимущества локальной сборки

- ✅ Работает 100% (без проблем с CI/CD)
- ✅ Быстро (не нужно ждать GitHub Actions)
- ✅ Полный контроль над процессом
- ✅ Можно отлаживать проблемы сразу
- ✅ Не зависит от конфигурации Gradle плагинов

## 🔄 Альтернатива: CI/CD

Если все же нужна автоматическая сборка, можно использовать:
- **GitLab CI** (бесплатно для приватных репозиториев)
- **CircleCI** (бесплатный план)
- **Bitrise** (специально для mobile)
- **AppCenter** (от Microsoft)

Но локальная сборка - самый надежный вариант!

