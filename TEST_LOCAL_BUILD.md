# 🧪 Тест локальной сборки

## Проверка: Работает ли сборка вообще?

Перед тем как менять CI/CD, нужно убедиться, что **локальная сборка работает**.

### Шаг 1: Проверка окружения

```powershell
# Проверка Node.js
node --version
# Должно быть: v18.x или выше

# Проверка Java
java -version
# Должно быть: openjdk version "17" или выше

# Проверка Android SDK
echo $env:ANDROID_HOME
# Должно быть что-то вроде: C:\Users\...\AppData\Local\Android\Sdk
```

### Шаг 2: Установка зависимостей

```powershell
cd C:\Users\shadiev-an\Documents\LB\PlazmaTelecomApp
npm install --legacy-peer-deps
```

**Если ошибка** - проблема в зависимостях проекта, не в сборке.

### Шаг 3: Создание keystore

```powershell
cd android
keytool -genkeypair -v -storetype PKCS12 `
    -keystore app\debug.keystore `
    -storepass android `
    -alias androiddebugkey `
    -keypass android `
    -keyalg RSA -keysize 2048 -validity 10000 `
    -dname "CN=Android Debug,O=Android,C=US"
```

### Шаг 4: Попытка сборки

```powershell
cd android
.\gradlew.bat assembleDebug
```

**Если сборка падает** - пришлите ошибку. Это поможет понять, в чем проблема.

**Если сборка успешна** - APK будет в `app\build\outputs\apk\debug\app-debug.apk`

---

## ❓ Если локальная сборка НЕ работает

Тогда проблема в самом проекте, а не в CI/CD. Нужно:

1. Проверить структуру проекта
2. Проверить зависимости
3. Возможно, пересоздать проект с нуля

---

## ✅ Если локальная сборка работает

Тогда проблема только в CI/CD. Решения:
- Использовать локальную сборку (рекомендуется)
- Использовать другой CI/CD (GitLab, Bitrise)
- Использовать Docker для сборки

