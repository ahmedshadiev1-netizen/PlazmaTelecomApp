# 🚀 Запуск Android приложения Плазмателеком

## ✅ Что уже готово:
- ✅ Node.js установлен (v22.21.0)
- ✅ Android Studio установлен
- ✅ Зависимости установлены
- ✅ Сервер API работает на `http://192.168.56.1:8000`
- ✅ Metro bundler запущен

## 📱 Следующие шаги:

### 1. **Откройте Android Studio**
- Запустите Android Studio
- Создайте виртуальное устройство (AVD) если еще не создано:
  - Tools → AVD Manager
  - Create Virtual Device
  - Выберите телефон (например, Pixel 4)
  - Выберите API Level 30+ (Android 11+)
  - Запустите эмулятор

### 2. **Добавьте Node.js в PATH**
Откройте PowerShell как администратор и выполните:
```powershell
$env:PATH += ";C:\Program Files\nodejs"
```

### 3. **Запустите приложение**
В новом терминале (с обновленным PATH):
```bash
cd C:\Users\shadiev-an\Desktop\lanbilling-client\PlazmaTelecomApp
npx react-native run-android
```

### 4. **Альтернативный способ - через Android Studio**
1. Откройте папку `android` в Android Studio
2. Дождитесь синхронизации Gradle
3. Нажмите "Run" (зеленая стрелка)

## 🔧 Если возникают проблемы:

### Проблема: "node не найден"
**Решение:**
```powershell
# Добавьте в PATH
$env:PATH += ";C:\Program Files\nodejs"
# Или перезапустите терминал
```

### Проблема: "Android SDK не найден"
**Решение:**
1. Откройте Android Studio
2. File → Settings → Appearance & Behavior → System Settings → Android SDK
3. Убедитесь, что SDK установлен
4. Добавьте в переменные окружения:
```
ANDROID_HOME = C:\Users\%USERNAME%\AppData\Local\Android\Sdk
```

### Проблема: "Metro bundler не запускается"
**Решение:**
```bash
# Очистите кеш
npx react-native start --reset-cache
```

## 📱 Тестирование приложения:

### 1. **Вход в систему:**
- Логин: `Test-Ne-Udalyat`
- Пароль: `Test-Ne-Udalyat`

### 2. **Проверьте функции:**
- ✅ Просмотр договоров (свайп влево/вправо)
- ✅ Просмотр услуг (свайп влево/вправо)
- ✅ Обещанные платежи
- ✅ Периодические услуги

### 3. **API подключение:**
- Приложение подключается к `http://192.168.56.1:8000`
- Убедитесь, что сервер запущен
- Проверьте в браузере: `http://192.168.56.1:8000/docs`

## 🎯 Готово к использованию!

Приложение полностью настроено и готово к тестированию на Android устройстве или эмуляторе.

