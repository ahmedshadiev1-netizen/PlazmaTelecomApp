rootProject.name = 'PlazmaTelecomApp'

pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
    // For React Native 0.72, try multiple possible plugin locations
    def reactNativePath = file("../node_modules/react-native")
    def gradlePluginPath = file("../node_modules/@react-native/gradle-plugin")
    
    if (gradlePluginPath.exists()) {
        // New location (RN 0.73+)
        includeBuild("../node_modules/@react-native/gradle-plugin")
    } else if (reactNativePath.exists()) {
        // Old location (RN 0.72)
        includeBuild("../node_modules/react-native")
    }
}

apply from: file("../node_modules/@react-native-community/cli-platform-android/native_modules.gradle"); applyNativeModulesSettingsGradle(settings)
include ':app'

