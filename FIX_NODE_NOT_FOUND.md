# 🔧 Исправление ошибки "Cannot run program 'node'"

## ❌ Проблема
Android Studio показывает ошибку:
- `Cannot run program "node"`
- `Не удается найти указанный файл`

Это означает, что Gradle не может найти Node.js.

## ✅ Решение 1: Настроить Node.js в Android Studio (РЕКОМЕНДУЕТСЯ)

### Шаг 1: Найти путь к Node.js

1. Откройте PowerShell или командную строку
2. Выполните команду:
   ```powershell
   where.exe node
   ```
   Или:
   ```powershell
   Get-Command node | Select-Object -ExpandProperty Source
   ```

3. Скопируйте путь (например: `C:\Program Files\nodejs\node.exe`)

### Шаг 2: Настроить в Android Studio

1. **File → Settings** (или `Ctrl+Alt+S`)
2. Перейдите в **Build, Execution, Deployment → Build Tools → Gradle**
3. В разделе **Gradle Projects** найдите **"android"**
4. Найдите поле **"Environment variables"** или **"Gradle JVM"**
5. Добавьте переменную окружения:
   - **Name:** `PATH`
   - **Value:** `C:\Program Files\nodejs;${PATH}`
   - (Замените путь на ваш путь к Node.js)

Или:

### Альтернатива: Настроить через gradle.properties

1. Откройте файл `android/gradle.properties`
2. Добавьте строку:
   ```properties
   org.gradle.nodejs.path=C:\\Program Files\\nodejs
   ```
   (Замените путь на ваш путь к Node.js, но БЕЗ `node.exe` в конце)

3. Синхронизируйте проект: **File → Sync Project with Gradle Files**

---

## ✅ Решение 2: Добавить Node.js в PATH системы

Если Node.js не в системном PATH:

1. **Win + R** → `sysdm.cpl` → Enter
2. Вкладка **"Дополнительно"** → **"Переменные среды"**
3. В разделе **"Системные переменные"** найдите **Path**
4. Нажмите **"Изменить"**
5. Нажмите **"Создать"** и добавьте путь к Node.js:
   - `C:\Program Files\nodejs`
   - Или `C:\Users\shadiev-an\AppData\Local\Programs\nodejs`
6. Нажмите **OK** во всех окнах
7. **Перезапустите Android Studio** (важно!)

---

## ✅ Решение 3: Использовать полный путь в local.properties

1. Откройте файл `android/local.properties`
2. Добавьте строку:
   ```properties
   node.path=C:\\Program Files\\nodejs\\node.exe
   ```
   (Замените на ваш путь)

3. Синхронизируйте проект

---

## 🔍 Как найти путь к Node.js

### Вариант A: Через PowerShell
```powershell
Get-Command node | Select-Object -ExpandProperty Source
```

### Вариант B: Через Проводник
1. Откройте Проводник
2. Перейдите в `C:\Program Files\nodejs\`
3. Или `C:\Users\shadiev-an\AppData\Local\Programs\nodejs\`
4. Найдите `node.exe`
5. Скопируйте полный путь

### Вариант C: Проверить через командную строку
```cmd
where node
```

---

## ✅ После исправления

1. **File → Sync Project with Gradle Files**
2. Дождитесь синхронизации
3. Ошибка должна исчезнуть

---

## 🎯 Проверка

После синхронизации внизу должно быть:
- ✅ "Gradle sync completed successfully"
- ✅ Без ошибок про "node"

---

## ⚠️ Важно

- Путь должен быть **БЕЗ пробелов** или в кавычках
- Используйте **двойные обратные слеши** `\\` в gradle.properties
- После изменения PATH **перезапустите Android Studio**

---

## 📝 Пример для gradle.properties

```properties
# Если Node.js в C:\Program Files\nodejs
org.gradle.nodejs.path=C:\\Program Files\\nodejs

# Или если в AppData
org.gradle.nodejs.path=C:\\Users\\shadiev-an\\AppData\\Local\\Programs\\nodejs
```

