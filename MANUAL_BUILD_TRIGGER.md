# 🚀 Как запустить сборку вручную

## Вариант 1: Через GitHub UI (Рекомендуется)

1. Откройте: https://github.com/ahmedshadiev1-netizen/PlazmaTelecomApp/actions

2. В левом меню найдите **"Build Android APK"**

3. Нажмите на **"Run workflow"** (справа вверху)

4. Выберите ветку **"main"**

5. Нажмите **"Run workflow"**

## Вариант 2: Через новый коммит

Создайте пустой коммит, чтобы запустить сборку:

```bash
git commit --allow-empty -m "Trigger build"
git push origin main
```

## Вариант 3: Проверьте статус

Если сборка не запускается автоматически:

1. Проверьте, что workflow файл в правильном месте:
   - `.github/workflows/build-android.yml`

2. Проверьте, что файл запушен:
   - https://github.com/ahmedshadiev1-netizen/PlazmaTelecomApp/blob/main/.github/workflows/build-android.yml

3. Проверьте, что ветка правильная:
   - Workflow настроен на `main` и `master`

## 🔍 Если сборка все еще не запускается:

1. Проверьте настройки репозитория:
   - Settings → Actions → General
   - Убедитесь, что Actions включены

2. Проверьте права доступа:
   - У вас должны быть права на запуск workflows

---

**Прямая ссылка для запуска вручную:**
https://github.com/ahmedshadiev1-netizen/PlazmaTelecomApp/actions/workflows/build-android.yml

