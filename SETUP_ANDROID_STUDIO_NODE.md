# 🔧 Настройка Node.js в Android Studio (ОБЯЗАТЕЛЬНО)

## ❌ Проблема
Ошибка: `Cannot run program "node"` - Android Studio не видит Node.js при запуске Gradle.

## ✅ Решение: Настроить через Android Studio Settings

Это самый надежный способ - Android Studio будет использовать правильный PATH.

### Шаг 1: Открыть настройки Gradle

1. **File → Settings** (или `Ctrl+Alt+S`)
2. Перейдите в **Build, Execution, Deployment → Build Tools → Gradle**

### Шаг 2: Настроить Environment Variables

1. В разделе **Gradle Projects** найдите проект **"android"**
2. Найдите раздел **"Environment variables"** (переменные окружения)
3. Если его нет, нажмите **"+"** чтобы добавить
4. Добавьте переменную:
   - **Name:** `PATH`
   - **Value:** `C:\Program Files\nodejs;%PATH%`
   
   Или если есть другие пути, добавьте Node.js в начало:
   ```
   C:\Program Files\nodejs;C:\Windows\system32;C:\Windows;...
   ```

### Шаг 3: Альтернатива - через Gradle JVM Settings

Если не нашли Environment variables, попробуйте:

1. В том же окне настроек найдите **"Gradle JVM"**
2. Рядом должна быть кнопка **"..."** или **"Edit"**
3. Найдите там настройки переменных окружения

### Шаг 4: Применить и перезапустить

1. Нажмите **Apply** → **OK**
2. **ВАЖНО:** Полностью закройте Android Studio
3. Запустите Android Studio снова
4. Откройте проект заново

### Шаг 5: Синхронизировать проект

1. **File → Sync Project with Gradle Files**
2. Дождитесь синхронизации

---

## ✅ Альтернативное решение: Добавить в системный PATH

Если настройки Android Studio не помогают:

1. **Win + R** → введите `sysdm.cpl` → Enter
2. Вкладка **"Дополнительно"** → **"Переменные среды"**
3. В разделе **"Системные переменные"** найдите **Path**
4. Нажмите **"Изменить"**
5. Нажмите **"Создать"** и добавьте:
   ```
   C:\Program Files\nodejs
   ```
6. Нажмите **OK** во всех окнах
7. **Полностью перезапустите Android Studio**
8. Синхронизируйте проект

---

## 🔍 Проверка

После перезапуска Android Studio:

1. **File → Sync Project with Gradle Files**
2. Внизу должно быть: ✅ "Gradle sync completed successfully"
3. Ошибка "Cannot run program 'node'" должна исчезнуть

---

## ⚠️ Важно

- **Обязательно перезапустите Android Studio** после изменения PATH
- Просто синхронизация не всегда помогает - нужен полный перезапуск
- Если не работает - проверьте, что путь к node.exe правильный:
  ```powershell
  Test-Path "C:\Program Files\nodejs\node.exe"
  ```
  Должно вернуть `True`

---

## 📝 Если ничего не помогает

1. Проверьте, что Node.js установлен:
   ```powershell
   node --version
   ```

2. Найдите полный путь:
   ```powershell
   Get-Command node | Select-Object -ExpandProperty Source
   ```

3. Используйте этот путь в настройках Android Studio

4. Или создайте bat-файл-обертку (последний вариант):
   - Создайте файл `android/node.bat`:
   ```batch
   @echo off
   "C:\Program Files\nodejs\node.exe" %*
   ```
   - И добавьте в PATH: `android` директорию

---

## 🎯 После успешной настройки

1. **Build → Build Bundle(s) / APK(s) → Build APK(s)**
2. APK будет готов через 2-5 минут

