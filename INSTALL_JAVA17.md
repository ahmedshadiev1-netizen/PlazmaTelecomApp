# 🔧 Установка Java 17 для сборки APK

## ❌ Проблема
У вас установлен Java 8, а Gradle 8.5 требует Java 17.

## ✅ Решение: Установить Java 17

### Вариант 1: Использовать JDK из Android Studio (БЫСТРО)

Android Studio уже содержит JDK 17! Нужно просто добавить его в PATH.

**В PowerShell (как администратор):**

```powershell
# Временно для текущей сессии
$env:JAVA_HOME="C:\Program Files\Android\Android Studio\jbr"
$env:PATH="$env:JAVA_HOME\bin;$env:PATH"

# Проверка
java -version
```

Должно показать Java 17.

**Для постоянного использования:**

1. **Win + R** → `sysdm.cpl` → Enter
2. Вкладка **"Дополнительно"** → **"Переменные среды"**
3. В разделе **"Системные переменные"**:
   - Нажмите **"Создать"**
   - **Имя:** `JAVA_HOME`
   - **Значение:** `C:\Program Files\Android\Android Studio\jbr`
   - Нажмите **OK**
4. Найдите переменную **Path** в системных переменных
5. Нажмите **"Изменить"** → **"Создать"**
6. Добавьте: `%JAVA_HOME%\bin`
7. Нажмите **OK** во всех окнах
8. **Перезапустите командную строку/PowerShell**

### Вариант 2: Скачать и установить JDK 17 отдельно

1. Скачайте JDK 17:
   - https://adoptium.net/temurin/releases/?version=17
   - Выберите: **Windows x64**, **JDK**, **17**, **MSI**

2. Установите (обычная установка)

3. После установки проверьте:
   ```powershell
   java -version
   ```

4. Если не работает, добавьте в PATH:
   - Обычно устанавливается в: `C:\Program Files\Eclipse Adoptium\jdk-17.x.x-hotspot\`
   - Добавьте `C:\Program Files\Eclipse Adoptium\jdk-17.x.x-hotspot\bin` в PATH

---

## ✅ Быстрое решение: Использовать скрипт

Я создал скрипт `build-with-java17.bat`, который автоматически использует Java 17 из Android Studio:

```powershell
cd C:\Users\shadiev-an\Documents\LB\PlazmaTelecomApp
.\build-with-java17.bat
```

Этот скрипт автоматически настроит Java 17 для сборки.

---

## 🔍 Проверка после установки

```powershell
java -version
```

Должно показать:
```
openjdk version "17.0.x"
```

Или:
```
java version "17.0.x"
```

---

## 🎯 После установки Java 17

1. Попробуйте сборку:
   ```powershell
   cd android
   .\gradlew.bat assembleDebug
   ```

2. Или используйте скрипт:
   ```powershell
   cd C:\Users\shadiev-an\Documents\LB\PlazmaTelecomApp
   .\build-with-java17.bat
   ```

---

## ⚠️ Важно

- **Не удаляйте Java 8** - некоторые программы могут использовать его
- **Java 17 и Java 8 могут сосуществовать** - просто используйте правильный для сборки
- **После установки перезапустите командную строку**

